;; Emacs configuration

;; Set convenient shortcuts for symbols absent from keyboards

(bind-key "C-c =" "≃")

;; All symbols below are already covered by the latin-1-prefix input method

;(bind-key "C-c A" "Å")
;(bind-key "C-c p" "±")
;(bind-key "C-c i" "í")

